[pytest]
# Run tests in verbose mode with short traceback by default
addopts = -v --tb=short

# Test discovery
testpaths = .
python_files = test_*.py
python_classes = TC*
python_functions = test_*

# Markers used in this project
markers =
    smoke: fast smoke-check tests
    functional: full end-to-end functional tests
